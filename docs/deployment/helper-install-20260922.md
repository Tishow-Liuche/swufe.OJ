# 外站提交前安装提醒（2026-09-22）

CF、洛谷、QOJ 提交前，前端通过同源同窗口带随机请求 ID 的 ping/pong 检测对应脚本。未检测到时打开安装弹窗，不调用提交接口、不清除已有结果，保留草稿。用户可以前往安装、重新检测或取消；重新检测成功不会自动提交。本地题不检测。

脚本版本：CF 7.6、洛谷 1.9、QOJ 2.6。旧版没有检测响应，需要更新。脚本在明确列出的 OJ 域名仅回应可用性，不读取 GM 存储、不交题、不显示提示条。可用性检测不是安全认证，也不能区分未安装、禁用、旧版或浏览器阻止运行，所以提示采用“未检测到可用脚本”。

## 验证

- 26 suites / 77 前端单测通过；类型检查与生产构建通过。
- 三个实际脚本的 OJ 域名握手浏览器测试通过。
- CF、洛谷、QOJ、本地题四个实际页面流程通过：缺脚本不发提交请求、保留代码、安装链接正确、重新检测不隐式提交、就绪后只提交一次。
- 增加弹窗居中检查，并视觉核对页面；原全局 margin reset 的影响已处理。
- 原洛谷语言选择 13 项与结果采集 14 项浏览器测试通过。
- 线上四个页面流程采用拦截 API 的浏览器测试，不修改正式提交。发布脚本内容逐字节与已测试文件相同。

测试通过直接注入实际 userscript 验证逻辑，没有自动操作用户本机 Tampermonkey 安装确认页；安装本身仍需浏览器用户确认。

## 发布

只发布前端和公开脚本，镜像 `swufe-oj-caddy:helper-install-20260922`；先增量复制带内容哈希的 assets，最后原子替换 index。保留旧 assets 和旧 index 备份，Caddy 未重启；后端、数据库和教师判题服务未改变。

线上安装地址：`https://test.singularitylab.online/install-oj-helpers.html`。
