# Avatar Recovery Plan

目标：修复上传过头像的用户因图片请求失败而永久显示姓名缩写的问题。服务器只读核实目标头像存在，新签名HTTPS URL返回200、JPEG 77785字节；原组件 error 后只设置 imageFailed，没有重试；签名有效期300秒。不能据此断言截图唯一原因就是过期。

方案：保留私有对象存储与签名机制，前端组件报告失败URL，auth store仅针对当前用户当前头像限次重新获取profile中的avatar；等待1.1秒跨越签名时间精度，合并并发恢复，30秒冷却，不修改其它资料，不因图片服务错误清除登录。顶部与个人中心读取同一头像和恢复版本。登出或换账号期间旧请求不可回写。

不修改后端权限、不公开存储桶、不改真实账号头像、不变更认证策略。

- [x] 新增组件/恢复状态测试和浏览器403→新图片成功用例，旧实现失败。
- [x] 实现UserAvatar load-error事件、auth recoverAvatar、App/Profile统一数据与事件绑定。
- [x] 全量前端43项测试、类型检查、生产构建通过；头像恢复、比赛布局及已有UI浏览器回归通过，独立审查无遗留重要问题。
- [x] 推送42411036，仅部署前端；真实临时账号验证头像图片naturalWidth>0并清理测试数据。

2026-09-19 上线验收：

- 运行版本 f4fe54d34e6cd7a94ac4d9e367c1ab494ce30491。
- 临时账号通过真实API上传PNG，拦截第一次头像请求返回403，随后浏览器重新获取签名并成功解码真实图片。
- 顶部与个人中心头像URL一致，刷新仍能显示；临时账号、比赛、头像对象及凭证文件已清理。
- 后端健康，判题worker 1，waiting/active/failed/paused均为0。
- 回滚镜像 swufe-oj-caddy:before-avatar-f4fe54d；源码备份 /home/ubuntu/oj-backups/before-avatar-f4fe54d.tgz。
- 独立审查发现的可选请求认证失败干扰登录、新上传头像被旧恢复冷却阻挡，均先添加失败回归再修复；其它请求认证策略不变。
